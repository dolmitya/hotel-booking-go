app:
  name: hotel-booking
  env: local
  http:
    host: 0.0.0.0
    port: 8080

postgres:
  host: localhost
  port: 5433
  database: booking
  username: booking
  password: booking
  ssl_mode: disable

kafka:
  bootstrap_servers:
    - localhost:9092
  topics:
    guest_events: guest-events
    booking_events: booking-events
    room_events: room-events
